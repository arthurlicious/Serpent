@page
@model SerpantWebApp.Pages.Audit.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AuditRecord[0].AuditActionType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AuditRecord[0].Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AuditRecord[0].DateTimeStamp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AuditRecord[0].KeyProductFieldID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AuditRecord[0].KeyRoleFieldID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AuditRecord) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AuditActionType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateTimeStamp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KeyProductFieldID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KeyRoleFieldID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AuditID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AuditID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AuditID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
